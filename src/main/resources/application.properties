# Server configuration
server.port=8081

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/pharmacie_db
spring.datasource.username=teemo
spring.datasource.password=123123gg**
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable open-in-view
spring.jpa.open-in-view=false

# Explicitly disable Spring Session
spring.session.jdbc.initialize-schema=always


# Désactiver la sécurité CSRF et les sessions explicites
spring.security.filter.dispatcher-types=REQUEST
spring.security.enable-csrf=false
